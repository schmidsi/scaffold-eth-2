{
  "scripts": {
    "account:generate": "echo 'DEPRECATED: use yarn foundry:account:generate instead' && yarn foundry:account:generate",
    "account:import": "echo 'DEPRECATED: use yarn foundry:account:import instead' && yarn foundry:account:import",
    "account": "echo 'DEPRECATED: use yarn foundry:account instead' && yarn foundry:account",
    "chain": "yarn foundry:chain",
    "compile": "echo 'DEPRECATED: use yarn foundry:compile instead' && yarn foundry:compile",
    "deploy:verify": "echo 'DEPRECATED: use yarn foundry:deploy:verify instead' && yarn foundry:deploy:verify",
    "deploy": "yarn foundry:deploy",
    "flatten": "echo 'DEPRECATED: use yarn foundry:flatten instead' && yarn foundry:flatten",
    "fork": "echo 'DEPRECATED: use yarn foundry:fork instead' && yarn foundry:fork",
    "format": "yarn next:format && yarn foundry:format",
    "foundry:account:generate": "yarn workspace @se-2/foundry account:generate",
    "foundry:account:import": "yarn workspace @se-2/foundry account:import",
    "foundry:account": "yarn workspace @se-2/foundry account",
    "foundry:chain": "yarn workspace @se-2/foundry chain",
    "foundry:compile": "yarn workspace @se-2/foundry compile",
    "foundry:deploy:verify": "yarn workspace @se-2/foundry deploy:verify",
    "foundry:deploy": "yarn workspace @se-2/foundry deploy",
    "foundry:fork": "yarn workspace @se-2/foundry fork",
    "foundry:format": "yarn workspace @se-2/foundry format",
    "foundry:lint": "yarn workspace @se-2/foundry lint",
    "foundry:test": "yarn workspace @se-2/foundry test",
    "generate": "echo 'DEPRECATED: use yarn foundry:account:generate instead' && yarn foundry:account:generate",
    "test": "yarn foundry:test",
    "verify": "echo 'DEPRECATED: use yarn foundry:verify instead' && yarn foundry:verify"
  }
}
